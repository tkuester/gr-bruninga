# auto-generated by grc.converter

id: bruninga_ax25_fsk_mod
label: AX.25 FSK Modulator
category: '[bruninga]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: int
    default: samp_rate
-   id: preamble_len_ms
    label: Preamble Len (ms)
    dtype: float
    default: '300'
-   id: flag_count
    label: Flag Count
    dtype: int
    default: '5'
-   id: mark_freq
    label: Mark Frequency (Hz)
    dtype: int
    default: '2200'
-   id: space_freq
    label: Space Frequency (Hz)
    dtype: int
    default: '1200'
-   id: baud_rate
    label: Baud Rate
    dtype: int
    default: '1200'

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.bruninga as bruninga
    make: bruninga.ax25_fsk_mod(${samp_rate}, ${preamble_len_ms}, ${flag_count}, ${mark_freq},
        ${space_freq}, ${baud_rate})

file_format: 1
