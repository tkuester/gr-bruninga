# auto-generated by grc.converter

id: bruninga_str_to_aprs
label: String to APRS
category: '[bruninga]'

parameters:
-   id: src
    label: Source
    dtype: string
-   id: dest
    label: Dest
    dtype: string
-   id: via
    label: Via
    dtype: raw

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import gnuradio.bruninga as bruninga
    make: bruninga.str_to_aprs(${src}, ${dest}, ${via})

file_format: 1
